import type { Metadata } from "next";
export const metadata: Metadata = { title: "NEXUS Models — Choose Your AI Companion", description: "Compare the NEXUS balanced and deep AI companion modes.", alternates: { canonical: "/models" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
