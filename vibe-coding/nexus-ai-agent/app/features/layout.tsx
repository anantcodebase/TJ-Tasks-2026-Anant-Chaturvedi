import type { Metadata } from "next";
export const metadata: Metadata = { title: "NEXUS Features — AI Tools for Creators and Learners", description: "Explore the core NEXUS AI tools for conversation, coding, learning, and productive work.", alternates: { canonical: "/features" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
