import type { Metadata } from "next";
export const metadata: Metadata = { title: "NEXUS Pricing — AI That Fits Your Workflow", description: "See the NEXUS interface pricing tiers and product limits before connecting a billing provider.", alternates: { canonical: "/pricing" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
