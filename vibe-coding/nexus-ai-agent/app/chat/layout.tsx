import type { Metadata } from "next";
export const metadata: Metadata = { title: "NEXUS Chat — Start Thinking", description: "Open the NEXUS AI chat interface and start a conversation.", alternates: { canonical: "/chat" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
