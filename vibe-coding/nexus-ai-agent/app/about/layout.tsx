import type { Metadata } from "next";
export const metadata: Metadata = { title: "NEXUS About — The Thinking Partner", description: "Learn about the design principles behind NEXUS AI and its human-first interface.", alternates: { canonical: "/about" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
